CREATE TABLE message (
                         id INT NOT NULL,
                         message VARCHAR(40000) NOT NULL,
                         show boolean,
                         PRIMARY KEY(id)
);